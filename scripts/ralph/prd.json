{
  "project": "btg-api-clientes-node",
  "branchName": "ralph/node-btg-api-client",
  "description": "TypeScript npm package at ./node providing strongly-typed Node.js access to BTG Pactual Auth, Position, and Operation APIs — a 1:1 port of the existing Python clients",
  "userStories": [
    {
      "id": "US-001",
      "title": "Initialize npm package scaffold",
      "description": "As a developer, I want a properly configured TypeScript npm package at ./node so consumers can install and import it.",
      "acceptanceCriteria": [
        "node/package.json created with \"type\": \"module\", \"engines\": { \"node\": \">=20\" }, entry points for ESM, and a build script",
        "node/tsconfig.json with strict: true, target: ES2022, module: NodeNext, moduleResolution: NodeNext, declaration: true, outDir: dist",
        "node/.gitignore excludes dist/ and node_modules/",
        "node/src/ directory created",
        "No runtime dependencies in package.json (only typescript and tsx as devDependencies; @types/node as devDependency)",
        "npx tsc --noEmit passes on an empty project",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "Port auth module (btgApiAuth)",
      "description": "As a developer, I want getAccessToken() and clearTokenCache() to obtain and cache OAuth2 tokens exactly like the Python client.",
      "acceptanceCriteria": [
        "node/src/btgApiAuth.ts exports getAccessToken(options?: { timeout?: number; useCache?: boolean }): Promise<string>",
        "node/src/btgApiAuth.ts exports clearTokenCache(): void",
        "node/src/btgApiAuth.ts exports BtgAuthError class with statusCode and responseBody properties",
        "node/src/btgApiAuth.ts exports TokenResponse interface",
        "Token cached in module-level state for TOKEN_VALIDITY_SECONDS - 60 seconds",
        "Reads BTG_CLIENT_ID / BTG_CLIENT_SECRET from process.env; throws BtgAuthError if missing",
        "Sends POST to https://api.btgpactual.com/iaas-auth/api/v1/authorization/oauth2/accesstoken with Basic auth header, x-id-partner-request UUID, Content-Type: application/x-www-form-urlencoded, body grant_type=client_credentials",
        "Reads token from access_token response header first, falls back to response JSON body",
        "tsc --noEmit passes",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": false,
      "notes": "Depends on US-001 scaffold"
    },
    {
      "id": "US-003",
      "title": "Port shared utilities (btgApiUtils)",
      "description": "As a developer, I need shared header-building, response-handling, and error utilities that mirror btg_api_utils.py.",
      "acceptanceCriteria": [
        "node/src/btgApiUtils.ts exports buildHeaders(extra?: Record<string, string>): Promise<Record<string, string>> — injects access_token, x-id-partner-request UUID, Content-Type: application/json",
        "node/src/btgApiUtils.ts exports handleResponse<T>(response: Response): Promise<T> — returns parsed JSON for 200, {} for 202, throws BtgApiError otherwise with message extracted from errors[0].message, meta.globalErrors[0].message, or message field",
        "node/src/btgApiUtils.ts exports BtgApiError class with statusCode and responseBody",
        "Exports BtgError, ErrorMetadata, ResponseError interfaces (ported from TypedDicts; Error renamed to BtgError)",
        "Uses crypto.randomUUID() for the partner-request ID",
        "tsc --noEmit passes",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": false,
      "notes": "Depends on US-002 (uses getAccessToken)"
    },
    {
      "id": "US-004",
      "title": "Port position request types and all nested response types",
      "description": "As a developer, I want every TypedDict from btg_api_position.py ported to a TypeScript interface so responses are fully typed.",
      "acceptanceCriteria": [
        "node/src/btgApiPositionTypes.ts contains TypeScript interfaces for every TypedDict in btg_api_position.py (~70 types)",
        "NotRequired[X] in Python maps to optional field (field?: X) in TypeScript",
        "list[X] maps to X[], dict maps to Record<string, unknown>",
        "No any types used",
        "tsc --noEmit passes",
        "Typecheck passes"
      ],
      "priority": 4,
      "passes": false,
      "notes": "Large story (~70 interfaces) but mechanical — all type definitions, no logic. Consult python/docs/API-POSITION.yml as source of truth."
    },
    {
      "id": "US-005",
      "title": "Port position API functions",
      "description": "As a developer, I want every position endpoint available as a typed async TS function.",
      "acceptanceCriteria": [
        "node/src/btgApiPosition.ts exports all 10 position functions with correct signatures (getPositionByAccount, getPositionByAccountAndDate, getPositionUnitPriceByAccount, getPositionUnitPriceHistoryByAccount, getPartnerPosition, getPositionRefresh, getPositionUnitPriceByAccountV2, getPositionUnitPriceHistoryByAccountV2, getPositionUnitPriceHistoryByPartnerV2, getPositionUnitPriceHistoryByAccountsV2)",
        "All URLs use BASE_URL = https://api.btgpactual.com/iaas-api-position and match the Python paths exactly",
        "HTTP methods and bodies match the Python client",
        "Timeouts implemented via AbortSignal.timeout(ms) with default 30000ms",
        "tsc --noEmit passes",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": false,
      "notes": "Depends on US-003 (utils) and US-004 (types)"
    },
    {
      "id": "US-006",
      "title": "Port operation module (btgApiOperation)",
      "description": "As a developer, I want every operation endpoint as a typed async TS function.",
      "acceptanceCriteria": [
        "node/src/btgApiOperation.ts exports OperationHistoryPartnerRequest interface and all 6 operation functions (getMovementsByAccountFull, getMovementsByAccountMonthly, getMovementsByAccountWeekly, getMovementsByPartnerAndPeriod, getMovementsByPartnerMonthly, getMovementsByPartnerWeekly)",
        "fetchCurrentAccount defaults to true; when true, adds fetch_current_account: no_value header",
        "URLs use BASE_URL = https://api.btgpactual.com/iaas-api-operation and match the Python paths exactly",
        "tsc --noEmit passes",
        "Typecheck passes"
      ],
      "priority": 6,
      "passes": false,
      "notes": "Depends on US-003 (utils)"
    },
    {
      "id": "US-007",
      "title": "Package barrel export",
      "description": "As a consumer, I want a single index.ts that re-exports everything so I can import from the package root.",
      "acceptanceCriteria": [
        "node/src/index.ts re-exports all public functions, types, and errors from the 4 modules",
        "package.json points main, module, and types at the compiled dist/index.js / dist/index.d.ts",
        "npm run build produces dist/ with .js and .d.ts files",
        "tsc --noEmit passes",
        "Typecheck passes"
      ],
      "priority": 7,
      "passes": false,
      "notes": "Depends on all source modules (US-002 through US-006)"
    },
    {
      "id": "US-008",
      "title": "Port test.py to test.ts",
      "description": "As a developer, I want a node/test.ts that demonstrates the package and mirrors python/test.py.",
      "acceptanceCriteria": [
        "node/test.ts imports getPositionByAccountAndDate, getMovementsByAccountFull, getMovementsByPartnerWeekly from the local package",
        "Mirrors the three branches in python/test.py (two behind if (false) gates, one active calling getMovementsByPartnerWeekly and writing response to get_movements_by_partner_weekly.json)",
        "Uses node:fs/promises writeFile with JSON.stringify",
        "Runnable via npx tsx node/test.ts (tsx is a devDependency)",
        "Type-checks strictly (no any) against the package types",
        "tsc --noEmit passes",
        "Typecheck passes"
      ],
      "priority": 8,
      "passes": false,
      "notes": "Depends on US-007 (barrel export)"
    },
    {
      "id": "US-009",
      "title": "Add minimal README",
      "description": "As a consumer of the package, I want a short node/README.md documenting setup and usage.",
      "acceptanceCriteria": [
        "node/README.md explains required env vars: BTG_CLIENT_ID, BTG_CLIENT_SECRET",
        "Notes that consumers must load .env themselves (no built-in dotenv)",
        "Shows install command and the npx tsx test.ts run command",
        "Shows one minimal import + call example",
        "Kept short — under ~40 lines"
      ],
      "priority": 9,
      "passes": false,
      "notes": "No code dependencies — just documentation"
    }
  ]
}
